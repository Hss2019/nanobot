# 2026-03-12 开发日志

## 目标
实现 nanobot 桌面应用（原生窗口 + 系统托盘 + WebUI 聊天界面）

## Round 1: 基础实现
- [x] 方案设计与决策 (ADR-001)
- [x] .docs 文档体系建立
- [x] Phase 1-6 基础代码

## Round 2: Bug 修复 + 完善

### Bug: 配置了 Key 也不能对话
- **根因**: desktop 模式没有 ChannelManager，outbound 消息堆在 MessageBus 里无人消费
- **修复**: `webui/server.py` 在 lifespan 中启动 `_dispatch_outbound()` 异步任务，消费 bus.outbound 并路由到 WebChannel

### Bug: 无 Key 无法启动
- **根因**: `_make_provider()` 找不到 API Key 时调用 `typer.Exit(1)` 直接退出
- **修复**: 新增 `_make_provider_safe()` 捕获 SystemExit，返回占位 LiteLLMProvider；前端显示黄色 banner 引导配置

### Bug: 进程杀不死
- **根因**: 多线程（uvicorn + pywebview + pystray）缺乏统一关闭机制
- **修复**: `desktop/app.py` 重写，添加 `_shutdown()` 集中关闭 + signal handler + atexit + 2s 强制 os._exit

### Enhancement: onboard --yes
- 新增 `-y/--yes` 参数跳过确认提示（refresh 模式）

### Enhancement: 完整管理界面
- 侧边栏导航：Chat / Settings / Sessions / Status
- Settings: 模型配置 + 全部 13 个 Provider Key + Tools 配置 + Channels 配置
- Sessions: 会话列表展示
- Status: 系统状态仪表板（连接/模型/Provider/Key/客户端数/会话数）
- 无 Key 时顶部黄色 banner 引导到 Settings

### Enhancement: PyInstaller 打包脚本
- `scripts/build_windows.py` — Windows 用户运行后生成 `dist/nanobot/nanobot.exe`

## 文件变更

| 文件 | 操作 | 说明 |
|------|------|------|
| `nanobot/webui/server.py` | 重写 | 添加 outbound dispatcher，has_key 字段 |
| `nanobot/webui/static/index.html` | 重写 | 完整管理界面（侧边栏 + 4 个面板） |
| `nanobot/desktop/app.py` | 重写 | 进程退出修复 |
| `nanobot/cli/commands.py` | 修改 | _make_provider_safe + onboard --yes + desktop 无 Key 提示 |
| `scripts/build_windows.py` | 新建 | PyInstaller 打包脚本 |
